# Copy this file to .env.local for local development
# (never commit .env.local — it's already in .gitignore via *.local)

# URL of your deployed Cloudflare Worker proxy.
# For local dev you can run the worker with: wrangler dev  (defaults to http://localhost:8787)
VITE_AI_PROXY_URL=http://localhost:8787
